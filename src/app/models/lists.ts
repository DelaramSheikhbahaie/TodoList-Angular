// var mongoose = require('mongoose')
//   , Schema = mongoose.Schema
export class List {
    _id;
    title: string;
    date: string;
    isMain: Boolean;
    // ListModel = mongoose.model('List', Schema({
    //     title: String,
    //     date: String,
    //     isMain: Boolean
    // }))
}